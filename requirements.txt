# Topes superiores obligatorios: sin ellos un `pip install` limpio salta de major
# y el arranque muere en tiempo de import. Ver el incidente del 05/09 (deploy
# mcp-fakespotter-00021: startup probe fallado por `mcp` 2.x).
mcp>=1.29.1,<2
httpx>=0.28.1,<1
yt-dlp>=2026.8.19,<2027
Pillow>=12.2.0,<13
python-dotenv>=1.2.3,<2
opencv-python-headless>=4.13.0.92,<5
numpy>=2.4.6,<3
uvicorn>=0.52.4,<1
# Dependencia directa: la importan los 6 modulos de src/tools/.
# Hoy funciona solo porque `mcp` la arrastra; si eso cambia, rompe sin aviso.
pydantic>=2.11,<3
